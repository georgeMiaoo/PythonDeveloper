# AI Agent 后端学习计划

> 目标：从编程基础开始，逐步具备使用 Python、FastAPI 独立开发、测试和部署 AI Agent 后端的能力。
>
> 预计周期：24 周，每周 8～10 小时。实际进度以完成阶段项目为准，不以赶完周数为准。

## 一、最终目标

完成本计划后，应当能够独立完成下面这些工作：

- 使用 Python 编写结构清晰、带类型标注的后端程序
- 使用 FastAPI 设计和实现 REST API
- 使用 PostgreSQL 设计用户、会话、消息、任务等数据模型
- 使用 SQLAlchemy 和 Alembic 完成数据库读写与迁移
- 接入大模型 API，实现普通响应和流式响应
- 编写 Agent 工具，并实现最小工具调用循环
- 保存会话上下文、长期记忆和 Agent 执行记录
- 处理异常、超时、重试、限流、权限和敏感数据
- 使用测试验证 API、工具和 Agent 行为
- 使用 Docker 将后端部署到服务器或云平台
- 根据日志、执行轨迹和评测结果定位问题

“独立开发”不等于能运行一个框架示例，而是能够解释系统为什么这样设计，并在发生错误时知道从哪里排查。

## 二、学习主线

```text
Python 基础
→ Web 与 HTTP
→ FastAPI
→ SQLite 与 SQL
→ PostgreSQL
→ 大模型 API
→ Agent 工具调用
→ 上下文与记忆
→ 评测、安全与部署
```

学习过程中不把 Python、FastAPI 和 Agent 分成三个互不相关的课程，而是围绕同一个项目逐步升级。

## 三、贯穿项目：个人知识库学习助理 Agent

这个项目负责把所有知识点串联起来。它最终可以：

- 管理学习任务和学习笔记
- 读取和搜索 Obsidian Markdown 笔记
- 根据笔记回答问题，并指出答案来源
- 根据学习进度生成练习题
- 检查答案，记录错题和薄弱点
- 根据目标创建学习计划
- 保存会话、工具调用和 Agent 运行记录
- 在执行写入、删除等操作前请求用户确认

项目按照下面的版本逐步演进：

### V0：命令行学习记录程序

```text
添加任务 → 查看任务 → 完成任务 → 搜索任务 → 保存到 JSON
```

这一版只使用 Python，不使用 FastAPI 和大模型。

### V1：学习任务 API

```text
POST   /tasks
GET    /tasks
GET    /tasks/{task_id}
PATCH  /tasks/{task_id}
DELETE /tasks/{task_id}
```

这一版学习 FastAPI、HTTP、Pydantic 和接口测试。

### V2：数据库版本

加入用户、任务、笔记、会话和消息，将数据从 JSON 文件迁移到数据库。

### V3：大模型聊天后端

```text
用户消息 → FastAPI → 模型 API → 流式返回 → 保存消息
```

这一版暂时是固定流程，不急着称为 Agent。

### V4：工具调用 Agent

Agent 可以根据问题自主决定是否调用工具：

```text
search_notes
read_note
get_learning_progress
create_quiz
check_answer
save_mistake
create_task
```

### V5：可部署的完整后端

增加鉴权、日志、评测、限流、安全控制、Docker 和生产数据库。

## 四、24 周阶段计划

| 阶段   |        周数 | 重点             | 阶段成果               |
| ---- | --------: | -------------- | ------------------ |
| 阶段 0 |     第 1 周 | 环境与编程入门        | 能独立运行和调试 Python 文件 |
| 阶段 1 |   第 2～5 周 | Python 核心基础    | 命令行学习记录程序          |
| 阶段 2 |   第 6～9 周 | HTTP 与 FastAPI | 学习任务 REST API      |
| 阶段 3 | 第 10～13 周 | SQL、数据库与测试     | PostgreSQL 数据后端    |
| 阶段 4 | 第 14～16 周 | 大模型 API        | 流式聊天后端             |
| 阶段 5 | 第 17～20 周 | Agent 核心       | 可自主调用工具的 Agent     |
| 阶段 6 | 第 21～24 周 | 生产工程           | 可测试、可部署的 Agent 后端  |

## 五、各阶段详细内容

### 阶段 0：环境与编程入门

#### 学习内容

- 终端、文件夹和文件路径
- Python 文件如何运行
- 编辑器、终端和 Python 解释器之间的关系
- 虚拟环境和依赖的基本概念
- 使用 `uv` 管理 Python 版本、虚拟环境和依赖
- 阅读错误信息和定位出错行
- Git 的最基本操作：状态、提交和查看历史

#### 阶段验收

- 能独立创建并运行一个 Python 文件
- 能给程序安装一个第三方依赖
- 能看懂错误堆栈中“错误类型、文件、行号”三个关键信息
- 能用 Git 保存一次可恢复的代码版本

### 阶段 1：Python 核心基础

#### 学习内容

- 变量与基本类型：字符串、整数、浮点数、布尔值、空值
- 容器类型：列表、元组、字典、集合
- 条件判断与循环
- 函数、参数、返回值和作用域
- 字符串、文件和 JSON
- 模块、包和导入
- 异常处理
- 类和对象的基础使用
- 类型标注
- `async` / `await` 的基本概念

#### 阶段项目

完成 V0 命令行学习记录程序：

- 创建任务
- 查看任务列表
- 修改任务状态
- 删除任务
- 搜索任务
- 保存和读取 JSON
- 对错误输入给出清楚提示

#### 阶段验收

- 不复制完整答案，能独立写出一个 100～200 行的小程序
- 能把一个大函数拆成多个职责明确的小函数
- 能解释列表与字典的使用场景
- 能为常用函数添加类型标注
- 能用 `try/except` 处理可预期错误

### 阶段 2：HTTP 与 FastAPI

#### 学习内容

- 客户端、服务器、请求和响应
- HTTP 方法：GET、POST、PUT、PATCH、DELETE
- URL、路径参数、查询参数和请求体
- JSON 与 HTTP 状态码
- FastAPI 路由
- Pydantic 请求和响应模型
- 数据校验
- 依赖注入
- 路由、业务逻辑和数据访问的职责分离
- 全局异常处理
- 自动 API 文档
- 使用 pytest 和测试客户端验证接口

#### 阶段项目

完成 V1 学习任务 API，并增加：

```text
GET  /health
POST /tasks
GET  /tasks
GET  /tasks/{task_id}
PATCH /tasks/{task_id}
DELETE /tasks/{task_id}
```

#### 阶段验收

- 能独立设计一组基本 REST API
- 能区分路径参数、查询参数和请求体
- 能正确使用常见的 2xx、4xx、5xx 状态码
- API 对非法输入能够返回稳定的错误结构
- 关键接口具有自动化测试

### 阶段 3：SQL、数据库与测试

#### 数据库路线

1. 先使用 SQLite 理解表、SQL、CRUD、事务和 ORM。
2. 基础稳定后切换 PostgreSQL。
3. 切换后，开发与生产尽量使用相同类型的数据库。
4. 暂时不同时学习 PostgreSQL 和 MySQL。

SQLite 用于降低入门阶段的环境负担；PostgreSQL 用于学习生产数据库、并发、JSONB，以及后续可能使用的 pgvector。MySQL 仍然是有效的生产选择，将来遇到公司技术栈或既有 MySQL 系统时再补充学习。

#### 学习内容

- 表、行、列、主键和外键
- `SELECT`、`INSERT`、`UPDATE`、`DELETE`
- 条件、排序、分页和关联查询
- 唯一约束与索引
- 事务、提交和回滚
- SQLAlchemy 2.x
- 数据库 Session
- 一对多关系
- Alembic 数据库迁移
- PostgreSQL 的安装、连接和权限
- 测试数据库和测试数据隔离

#### 初始数据模型

```text
User
Task
Note
Conversation
Message
AgentRun
ToolCall
```

#### 阶段验收

- 能独立设计上述数据表及其关系
- 能解释为什么需要主键、外键、唯一约束和索引
- 能用事务完成一组必须共同成功或失败的操作
- 能创建、执行和回滚数据库迁移
- 能把项目从 SQLite 切换到 PostgreSQL 并通过测试

### 阶段 4：大模型 API

#### 学习内容

- 大模型请求和响应结构
- System、User、Assistant 等消息角色
- 环境变量与 API 密钥管理
- 同步调用与异步调用
- 流式响应和 SSE
- 结构化输出
- 上下文窗口与消息裁剪
- 超时、重试和取消
- Token、延迟和费用记录
- 模型服务异常与应用异常的区分

#### 阶段项目

完成 V3 聊天后端：

```text
POST /conversations
POST /conversations/{id}/messages
GET  /conversations/{id}/messages
POST /chat/stream
```

#### 阶段验收

- 能保存并恢复多轮对话
- 能实现稳定的流式返回
- API 密钥不会写入代码或提交到版本库
- 能处理模型超时、限流和返回格式异常
- 能记录每次调用的模型、耗时和 Token 使用量

### 阶段 5：Agent 核心

#### 核心判断

固定的“搜索文档 → 调用模型 → 返回答案”是工作流。只有当模型可以在运行时决定是否调用工具、调用哪个工具、是否继续以及何时停止时，才进入 Agent 的核心部分。

#### 学习内容

- Function Calling / Tool Calling
- 工具定义与参数 Schema
- 工具注册和执行
- 最小 Agent 循环
- 最大循环次数
- 单步和整次运行超时
- 工具错误返回
- 幂等性
- Agent 状态与执行轨迹
- 人工确认与暂停恢复
- 先手写最小循环，再学习 Agent 框架

#### 最小 Agent 循环

```text
接收用户问题
→ 把可用工具提供给模型
→ 模型决定是否调用工具
→ 后端校验并执行工具
→ 将结果交还模型
→ 模型决定继续调用或生成答案
→ 达到答案、次数或时间边界后停止
```

#### 阶段验收

- 能不依赖复杂框架实现一个最小工具调用循环
- 能校验工具参数和处理工具执行失败
- 能防止 Agent 无限循环
- 写入、删除和外部操作具有人工确认边界
- 能从数据库还原一次 Agent 运行过程

### 阶段 6：记忆、评测、安全与部署

#### 记忆与检索

记忆不等于 RAG，也不等于把所有消息存进向量数据库。按照数据性质选择存储方式：

| 数据 | 初始方案 |
|---|---|
| 用户姓名、偏好、当前状态 | PostgreSQL 结构化字段 |
| 最近对话 | 会话消息与上下文窗口 |
| 学习任务、错题、进度 | PostgreSQL 关系表 |
| 历史笔记 | 全文搜索和条件查询 |
| 语义相近的笔记片段 | 评测证明有必要后使用 pgvector |
| PDF、图片等原始文件 | 文件系统或对象存储 |

长期记忆需要支持来源、创建时间、更新时间、失效、修改和删除，不能只支持不断添加。

#### 评测

建立一组固定测试案例，至少覆盖：

- 是否选择了正确工具
- 是否提供了正确工具参数
- 是否错误调用了危险工具
- 是否正确使用笔记来源
- 信息不足时是否会承认不知道
- 多轮对话是否保持上下文
- 失败后是否能够安全停止
- 延迟和 Token 成本是否可接受

生产中的失败案例应补充到回归测试集中。

#### 安全与生产工程

- 用户认证与权限控制
- API 限流
- Prompt Injection 基础防护
- 敏感信息与日志脱敏
- 数据访问范围控制
- CORS 配置
- 结构化日志
- 健康检查
- Docker
- 配置管理
- 数据库备份
- 部署和回滚
- 根据需要再引入 Redis

#### 阶段验收

- 新用户可以注册、登录并访问自己的会话
- 不同用户的数据不会互相泄露
- Agent 的重要操作有日志和可追踪记录
- 固定评测集可以重复执行
- 项目可以通过 Docker 在新环境启动
- 能根据日志排查一次真实错误

## 六、推荐技术栈

技术栈随学习阶段逐步引入，不需要第一天全部安装。

```text
Python
uv
FastAPI
Pydantic
SQLAlchemy 2.x
Alembic
SQLite
PostgreSQL
pytest
HTTPX
大模型官方 Python SDK
Docker
pgvector（按需）
Redis（按需）
```

Agent 框架不在最初的必选项中。手写并理解最小 Agent 循环后，再根据项目复杂度评估 OpenAI Agents SDK、LangGraph 或其他框架。

## 七、每周学习节奏

每周 8～10 小时可以这样分配：

| 活动 | 比例 | 目的 |
|---|---:|---|
| 阅读和理解 | 30% | 建立基本概念 |
| 亲手编码 | 50% | 把知识放进项目 |
| 调试、测试、复盘 | 20% | 形成独立解决问题的能力 |

每个知识点遵循同一个循环：

```text
理解概念
→ 写最小示例
→ 放进主项目
→ 主动测试错误情况
→ 编写测试
→ 用自己的话记录原因
```

每周至少保留下面四项记录：

- 本周完成了什么可运行功能
- 遇到了什么错误
- 错误的真正原因是什么
- 下周准备验证什么

## 八、第一周执行计划

### 第 1 天：认识运行环境

- 认识终端、路径和当前目录
- 查看 Python 是否安装
- 创建第一个 Python 文件
- 运行并修改输出内容

成果：独立运行 `hello.py`。

### 第 2 天：变量和基本类型

- 字符串、整数、浮点数、布尔值
- `print()` 和基本运算
- 使用变量保存任务名称和完成状态

成果：打印一条结构清楚的学习任务。

### 第 3 天：输入、条件和错误

- `input()`
- `if/elif/else`
- 将输入转换为数字
- 观察并理解一次类型转换错误

成果：根据用户输入创建或拒绝一条任务。

### 第 4 天：列表和循环

- 创建任务列表
- 添加任务
- 遍历并编号显示任务

成果：可以连续管理多条内存中的任务。

### 第 5 天：函数

- 定义函数
- 参数和返回值
- 将添加、显示任务拆成不同函数

成果：不把所有代码堆在一个位置。

### 第 6 天：小项目整合

完成一个可以循环运行的命令行任务管理器：

```text
1. 添加任务
2. 查看任务
3. 完成任务
4. 退出程序
```

### 第 7 天：复盘与重写

- 不看原代码重新实现主要功能
- 为异常输入增加处理
- 记录本周最难理解的三个问题
- 提交第一个可运行版本

## 九、暂时不优先学习的内容

在主项目出现真实需要之前，暂时不把时间投入到：

- 算法竞赛和大量刷题
- 数据分析、爬虫和桌面 GUI
- Python 高级元编程
- 微服务和 Kubernetes
- 多 Agent 协作
- 复杂 LangGraph 状态图
- 一开始就安装专用向量数据库
- 同时接入多个 Agent 框架
- 没有评测依据的复杂 RAG

这些内容并非不重要，而是需要在合适的阶段学习。

## 十、学习过程中的 AI 使用边界

AI 可以帮助：

- 解释概念和错误信息
- 提供小型示例
- 检查代码和测试
- 提问和出练习题
- 比较不同设计方案

学习者需要亲自完成：

- 关键函数和核心业务逻辑
- 对错误原因的解释
- 数据模型和接口设计决策
- 测试用例
- 阶段项目的重新实现

如果一段代码能够运行但无法解释，就暂时不算掌握。

## 十一、参考资料

- [Python 官方教程](https://docs.python.org/3/tutorial/)
- [FastAPI 官方教程](https://fastapi.tiangolo.com/tutorial/)
- [Pydantic 官方文档](https://docs.pydantic.dev/latest/)
- [SQLAlchemy 2.0 官方教程](https://docs.sqlalchemy.org/en/20/tutorial/)
- [PostgreSQL 官方文档](https://www.postgresql.org/docs/current/)
- [pytest 官方文档](https://docs.pytest.org/)

参考资料用于解决当前问题，不以从头到尾读完文档为学习目标。

## 十二、总进度清单

- [ ] 能独立运行和调试 Python 程序
- [ ] 完成命令行学习记录程序
- [ ] 完成 FastAPI 学习任务 API
- [ ] 掌握 SQLite 和基本 SQL
- [ ] 将项目切换到 PostgreSQL
- [ ] 完成数据库迁移和接口测试
- [ ] 接入大模型并实现流式聊天
- [ ] 独立实现最小 Agent 工具调用循环
- [ ] 完成知识库搜索工具
- [ ] 建立会话和长期记忆边界
- [ ] 建立 Agent 固定评测集
- [ ] 完成鉴权、限流和安全控制
- [ ] 使用 Docker 部署完整后端
- [ ] 独立完成一次线上问题排查
