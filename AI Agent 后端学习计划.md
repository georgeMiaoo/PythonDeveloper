# AI Agent 后端学习计划

> 目标：从编程基础开始，逐步具备使用 Python、FastAPI 独立开发、测试和部署 AI Agent 后端的能力。
>
> 预计周期：24 周，每周 8～10 小时。实际进度以完成阶段项目为准，不以赶完周数为准。

## 一、最终目标

完成本计划后，应当能够独立完成下面这些工作：

- 使用 Python 编写结构清晰、带类型标注的后端程序
- 使用 FastAPI 设计和实现 REST API
- 使用 PostgreSQL 设计用户、会话、消息、任务等数据模型
- 使用 SQLAlchemy 和 Alembic 完成数据库读写与迁移
- 接入大模型 API，实现普通响应和流式响应
- 编写 Agent 工具，并实现最小工具调用循环
- 保存会话上下文、长期记忆和 Agent 执行记录
- 处理异常、超时、重试、限流、权限和敏感数据
- 使用测试验证 API、工具和 Agent 行为
- 使用日志、指标、Trace 和评测定位质量与性能问题
- 建立基础 CI/CD，自动完成检查、测试、部署验证和回滚
- 使用 Docker 将后端部署到服务器或云平台
- 根据日志、执行轨迹和评测结果定位问题

“独立开发”不等于能运行一个框架示例，而是能够解释系统为什么这样设计，并在发生错误时知道从哪里排查。

## 二、学习主线

```text
Python 基础
→ Web 与 HTTP
→ FastAPI
→ SQLite 与 SQL
→ PostgreSQL
→ 大模型 API
→ Agent 工具调用
→ 上下文与记忆
→ 评测、安全与部署
```

学习过程中不把 Python、FastAPI 和 Agent 分成三个互不相关的课程，而是围绕同一个项目逐步升级。

## 三、起点差异：前端背景如何影响这份计划

学习者已经能够独立完成完整的前端项目，Python 语言本身零基础。这意味着计划中"后端通用概念"与"Python 语言和生态"两部分的难度完全不同，排期需要相应调整。

### 已经具备、不需要重新建立的概念

HTTP 方法与状态码、JSON、REST 接口设计、URL 与查询参数、CORS、终端与路径、Git、包管理器与 lock 文件、环境变量与配置分离。

这些内容在阶段 0 和阶段 2 中占了相当比重，但对本学习者只是"换一种写法"，不需要重新理解。相关章节按"确认对应工具"处理，不按"从零讲解"处理。

### 概念对应关系

| 已掌握 | Python 侧对应 | 说明 |
|---|---|---|
| npm / package.json / lock 文件 | uv / pyproject.toml / uv.lock | 概念一致 |
| Prettier + ESLint | Ruff | 一个工具同时负责格式化与静态检查 |
| TypeScript 类型 | 类型标注 + mypy 或 pyright | 语法不同，思路一致 |
| zod | Pydantic | 运行时校验，定位相同 |
| Express / Hono 路由 | FastAPI 路由 | 装饰器写法，校验内置 |
| fetch | httpx | 同步与异步两套接口 |
| Vitest / Jest | pytest | fixture 是新概念 |

### 需要重点投入的部分

- **SQL 与关系数据库（阶段 3）**：前端经验在这里几乎没有迁移价值，而且 SQLAlchemy 2.x 的抽象层次高于前端常用的 Prisma、Drizzle。这一阶段不压缩。
- **Agent 核心与生产工程（阶段 5、6）**：与前端经验无关，是全计划最难、也最有价值的部分。从阶段 0～2 节省下来的时间全部投入到这里。

### 前端背景特有的五个陷阱

1. **`async` 是同一个词，语义不同。** JavaScript 中不存在同步的 `fetch`，无法"意外阻塞"。Python 中同步与异步是并存的两套世界，在 `async def` 路由里调用 `requests.get()` 或 `time.sleep()` 会阻塞事件循环，导致该进程上所有并发请求一起卡住。这是前端转 Python 后端最高频的生产事故，见阶段 2 的"异步与阻塞边界"。
2. **类型标注在运行时不生效。** TypeScript 在编译期强制检查，Python 的类型标注默认只是注释，不主动运行 mypy 或 pyright 就没有任何约束。真正在运行时校验数据的是 Pydantic。需要把"类型检查"和"数据校验"当作两件独立的事。
3. **import 系统比 ESM 复杂。** 相对导入、`__init__.py`、循环导入和 `sys.path`，都没有 `package.json` 的 exports 那样清晰的边界。第一次组织多文件项目时会遇到问题，属于正常现象。
4. **虚拟环境不等于 `node_modules`。** npm 天然按项目隔离依赖，Python 是全局解释器加显式激活的虚拟环境。`uv` 缓解了大部分问题，但必须始终清楚"当前代码运行在哪个解释器上"。
5. **可变默认参数。** `def f(items=[])` 中的默认值在所有调用之间共享，是 Python 特有的陷阱，JavaScript 中没有对应物。

### 交付上的优势

具备前端能力意味着可以在两天内为 Agent 后端接上一个真正可用的聊天界面并部署上线，几乎没有额外学习成本。

建议在阶段 4 完成流式聊天后端后立即接入前端，不要推迟到计划末尾。这会把最终产出从"一个只能在本地运行的后端项目"变成"一个可以直接打开使用的完整产品"，是这份计划中性价比最高的一步。

## 四、贯穿项目：个人知识库学习助理 Agent

这个项目负责把所有知识点串联起来。它最终可以：

- 管理学习任务和学习笔记
- 读取和搜索 Obsidian Markdown 笔记
- 根据笔记回答问题，并指出答案来源
- 根据学习进度生成练习题
- 检查答案，记录错题和薄弱点
- 根据目标创建学习计划
- 保存会话、工具调用和 Agent 运行记录
- 在执行写入、删除等操作前请求用户确认

项目按照下面的版本逐步演进：

### V0：命令行学习记录程序

```text
添加任务 → 查看任务 → 完成任务 → 搜索任务 → 保存到 JSON
```

这一版只使用 Python，不使用 FastAPI 和大模型。

### V1：学习任务 API

```text
POST   /tasks
GET    /tasks
GET    /tasks/{task_id}
PATCH  /tasks/{task_id}
DELETE /tasks/{task_id}
```

这一版学习 FastAPI、HTTP、Pydantic 和接口测试。

### V2：数据库版本

加入用户、任务、笔记、会话和消息，将数据从 JSON 文件迁移到数据库。

### V3：大模型聊天后端

```text
用户消息 → FastAPI → 模型 API → 流式返回 → 保存消息
```

这一版暂时是固定流程，不急着称为 Agent。

### V4：工具调用 Agent

Agent 可以根据问题自主决定是否调用工具：

```text
search_notes
read_note
get_learning_progress
create_quiz
check_answer
save_mistake
create_task
```

### V5：可部署的完整后端

增加鉴权、可观测性、评测、限流、安全控制、CI/CD、Docker 和生产数据库。

## 五、24 周阶段计划

| 阶段 | 周数 | 重点 | 阶段成果 |
|---|---:|---|---|
| 阶段 0 | 第 1 周第 1～2 天 | 环境与工具链 | 能独立创建、运行和管理 Python 项目 |
| 阶段 1 | 第 1 周第 3 天～第 3 周 | Python 核心基础 | 命令行学习记录程序 |
| 阶段 2 | 第 4～5 周 | HTTP 与 FastAPI | 学习任务 REST API |
| 阶段 3 | 第 6～9 周 | SQL、数据库与测试 | PostgreSQL 数据后端 |
| 阶段 4 | 第 10～12 周 | 大模型 API | 流式聊天后端（并接入前端界面） |
| 阶段 5 | 第 13～18 周 | Agent 核心与评测 | 可自主调用工具并恢复运行的 Agent，以及可重复执行的评测集 |
| 阶段 6 | 第 19～24 周 | 生产工程 | 可观测、可测试、可部署的 Agent 后端 |

排期依据第三节的起点差异做过调整，总周数不变，权重重新分配：

- **阶段 0 压缩到 2 天**：终端、路径、Git 和包管理概念已经具备，只需确认 Python 生态中的对应工具。
- **阶段 1 压缩到约 2.5 周**：控制流、函数、模块化等编程概念已经具备，新增的只有 Python 语法与标准库习惯。
- **阶段 2 压缩到 2 周**：HTTP 与 REST 部分不需要重讲，真正新增的是依赖注入、`lifespan` 和异步阻塞边界。
- **阶段 3 保持 4 周**：这是唯一一块完全陌生且必须硬啃的内容，不压缩。
- **阶段 5、6 各增加 2 周**：吸收前面节省的时间，用于 Agent 循环、评测、可观测性和部署。

## 六、各阶段详细内容

### 阶段 0：环境与工具链

本阶段大部分概念已经在前端工程中建立，重点是确认 Python 生态中的对应工具，而不是从零讲解。计划 2 天完成。

> **AI 强度：放开** —— 让 AI 用前端类比讲解工具链、排查安装问题；命令自己亲手跑一遍。详见第十一节。

#### 学习内容

- 终端、文件夹和文件路径
- Python 文件如何运行
- 编辑器、终端和 Python 解释器之间的关系
- 虚拟环境和依赖的基本概念
- 使用 `uv` 管理 Python 版本、虚拟环境和依赖
- 认识 `pyproject.toml`、`uv.lock` 和可重复安装
- 区分项目依赖和开发依赖
- 使用 Ruff 进行格式化和基础静态检查
- 认识 `.gitignore`，避免提交 `.env`、虚拟环境和密钥
- 建立最小项目目录结构
- 阅读错误信息和定位出错行
- Git 的最基本操作：状态、提交和查看历史

#### 阶段验收

- 能独立创建并运行一个 Python 文件
- 能给程序安装一个第三方依赖
- 能解释 `pyproject.toml` 和 `uv.lock` 分别解决什么问题
- 能运行格式化和基础代码检查
- 能看懂错误堆栈中“错误类型、文件、行号”三个关键信息
- 能用 Git 保存一次可恢复的代码版本

### 阶段 1：Python 核心基础

> **AI 强度：最严** —— 全部代码自己写，AI 只做解释错误、出题、埋 bug 和审阅。每天贴代码问"哪里不像 Python"。详见第十一节。

#### 学习内容

- 变量与基本类型：字符串、整数、浮点数、布尔值、空值
- 容器类型：列表、元组、字典、集合
- 条件判断与循环
- 函数、参数、返回值和作用域
- 字符串、文件和 JSON
- 使用 `pathlib` 处理文件和 Obsidian 路径
- 模块、包和导入
- 异常处理
- 类和对象的基础使用
- `dataclass` 的基本使用
- 上下文管理器和 `with`
- 迭代器、生成器的基本概念
- 类型标注
- 使用标准库 `logging` 记录运行信息
- `async` / `await` 的基本概念
- 从小函数开始编写 pytest 测试

#### 阶段项目

完成 V0 命令行学习记录程序：

- 创建任务
- 查看任务列表
- 修改任务状态
- 删除任务
- 搜索任务
- 保存和读取 JSON
- 对错误输入给出清楚提示

#### 阶段验收

- 不复制完整答案，能独立写出一个 100～200 行的小程序
- 能把一个大函数拆成多个职责明确的小函数
- 能解释列表与字典的使用场景
- 能为常用函数添加类型标注
- 能用 `try/except` 处理可预期错误
- 能使用 `pathlib` 读取和保存文件
- 能为核心函数编写正常、异常和边界测试

### 阶段 2：HTTP 与 FastAPI

> **AI 强度：严** —— 路由、请求响应模型和错误结构自己设计；配置与测试脚手架可以让 AI 写。先自己设计完 API 再让它评审。详见第十一节。

#### 学习内容

- 客户端、服务器、请求和响应
- HTTP 方法：GET、POST、PUT、PATCH、DELETE
- URL、路径参数、查询参数和请求体
- JSON 与 HTTP 状态码
- FastAPI 路由
- Pydantic 请求和响应模型
- 使用 Pydantic Settings 管理不同环境的配置
- 数据校验
- 依赖注入
- 使用 `lifespan` 管理数据库、HTTP Client 等共享资源
- Middleware、CORS 和 Request ID
- 同步函数、异步函数和阻塞操作的边界（重点，见下）
- 请求取消和客户端断开处理
- 路由、业务逻辑和数据访问的职责分离
- 全局异常处理
- 分页与文件上传基础
- 自动 API 文档
- 使用 pytest 和测试客户端验证接口

#### 异步与阻塞边界

这一节单独列出，因为它是本计划中最容易造成生产事故的部分，而且前端经验会在这里产生误导：JavaScript 中不存在同步阻塞的网络调用，Python 中存在。

需要能够回答：

- 事件循环是什么，同一个进程上的所有请求为什么共享它
- 在 `async def` 中调用同步阻塞函数会发生什么
- 哪些常见调用是阻塞的：`requests`、`time.sleep`、同步数据库驱动、文件读写、CPU 密集计算
- 对应的异步替代：`httpx.AsyncClient`、`asyncio.sleep`、异步数据库驱动
- 无法替换为异步版本时，如何用线程池隔离（`run_in_threadpool` 或 `asyncio.to_thread`）
- 什么时候应该直接写普通的 `def` 路由，交给 FastAPI 自行放进线程池
- `asyncio.gather` 与 `Promise.all` 的相同点和不同点

验证方式：先写一个包含阻塞调用的路由，用并发请求观察吞吐塌陷，再改成异步版本对比。没有亲手做过这个实验，不算掌握。

#### 阶段项目

完成 V1 学习任务 API，并增加：

```text
GET  /health
POST /tasks
GET  /tasks
GET  /tasks/{task_id}
PATCH /tasks/{task_id}
DELETE /tasks/{task_id}
```

#### 阶段验收

- 能独立设计一组基本 REST API
- 能区分路径参数、查询参数和请求体
- 能正确使用常见的 2xx、4xx、5xx 状态码
- API 对非法输入能够返回稳定的错误结构
- 能说明一个共享资源为什么要在应用启动和关闭时管理
- 能演示一次阻塞调用导致的并发吞吐塌陷，并说明修复方式
- 能避免在异步路由中直接执行长时间阻塞操作
- 关键接口具有自动化测试

### 阶段 3：SQL、数据库与测试

> **AI 强度：中** —— 表结构、关系和索引决策自己做；查询、迁移脚本和测试数据可以让 AI 写，但迁移脚本必须逐行读懂后才执行。详见第十一节。

#### 数据库路线

1. 先使用 SQLite 理解表、SQL、CRUD、事务和 ORM。
2. 基础稳定后切换 PostgreSQL。
3. 切换后，开发与生产尽量使用相同类型的数据库。
4. 暂时不同时学习 PostgreSQL 和 MySQL。

SQLite 用于降低入门阶段的环境负担；PostgreSQL 用于学习生产数据库、并发、JSONB，以及后续可能使用的 pgvector。MySQL 仍然是有效的生产选择，将来遇到公司技术栈或既有 MySQL 系统时再补充学习。

#### 学习内容

- 表、行、列、主键和外键
- `SELECT`、`INSERT`、`UPDATE`、`DELETE`
- 条件、排序、分页和关联查询
- 唯一约束与索引
- 事务、提交和回滚
- SQLAlchemy 2.x
- 数据库 Engine、连接池和 Session
- 每个请求独立管理 Session 生命周期
- `AsyncSession` 不能在并发任务之间共享
- 一对多关系
- 事务隔离、并发冲突与锁的基础
- N+1 查询及常见加载策略
- 分页、索引和基础查询分析
- 统一使用 UTC 保存时间
- Alembic 数据库迁移
- 检查自动生成的迁移脚本
- PostgreSQL 的安装、连接和权限
- 测试数据库和测试数据隔离
- 数据库备份和恢复演练

#### 初始数据模型

```text
User
Task
Note
Conversation
Message
AgentRun
ToolCall
```

#### 阶段验收

- 能独立设计上述数据表及其关系
- 能解释为什么需要主键、外键、唯一约束和索引
- 能用事务完成一组必须共同成功或失败的操作
- 能正确管理每个请求的数据库 Session
- 能发现并修复一个 N+1 查询问题
- 能解释并处理一次唯一约束或并发更新冲突
- 能创建、执行和回滚数据库迁移
- 能把项目从 SQLite 切换到 PostgreSQL 并通过测试
- 能完成一次数据库备份与恢复

### 阶段 4：大模型 API

> **AI 强度：中高** —— SDK 调用、流式骨架和前端界面都可以交给 AI；模型访问层的边界、超时重试策略和缓存前缀排列自己决定。缓存命中必须自己验证。详见第十一节。

#### 学习内容

- 大模型请求和响应结构
- System、User、Assistant 等消息角色
- 环境变量与 API 密钥管理
- 同步调用与异步调用
- 建立独立的模型访问层，避免业务代码散落 SDK 调用
- 流式响应和 SSE
- 流式连接断开后的取消与资源释放
- 结构化输出
- 上下文窗口与消息裁剪
- Prompt Caching 与缓存前缀设计（重点，见下）
- 超时、重试和取消
- 重试与幂等性的关系
- Prompt 模板、参数和版本记录
- Token、延迟和费用记录
- 模型服务异常与应用异常的区分
- 在测试中模拟模型响应

#### Prompt Caching

每次请求都要把完整的 System Prompt、工具定义和全部历史消息重新发给模型，并按输入 Token 全额计费。Prompt Caching 让服务端缓存这段重复的前缀，命中后按大幅折扣计价。对 Agent 来说影响尤其大——工具定义和 System Prompt 又长又几乎不变，却在每一轮循环里重复发送。

各家厂商的机制不同，有的需要显式标记缓存点，有的自动生效。以 Claude 为例，需要理解的核心规则：

- **缓存按前缀匹配**：前缀中任何一个字节变化，都会让该位置之后的全部缓存失效
- **渲染顺序是 `tools` → `system` → `messages`**：稳定内容必须在物理位置上排在易变内容之前
- **写入比正常输入贵**（5 分钟有效期约 1.25 倍，1 小时约 2 倍），**读取则大幅便宜**（约 0.1 倍）。5 分钟档两次请求即可回本，1 小时档需要三次
- **有最小可缓存长度**，随模型不同在 512～4096 Token 之间；低于阈值不会报错，只是静默不缓存
- **缓存与模型绑定**：中途切换模型，缓存全部作废

最常见的失效原因是把动态内容放进了 System Prompt：

| 写法 | 后果 |
|---|---|
| System Prompt 里插入 `datetime.now()` | 每次请求前缀都不同，缓存永远不命中 |
| 插入用户名、会话 ID 等每次不同的值 | 同上 |
| `json.dumps()` 未指定 `sort_keys=True` | 序列化顺序不稳定，字节不一致 |
| 工具列表按条件动态增减或顺序不固定 | 工具排在最前面，一变全部作废 |

正确做法是保持 System Prompt 和工具定义完全冻结，把动态上下文放到 `messages` 靠后的位置。

**验证方式**：读取响应中的缓存用量字段（Claude 是 `usage.cache_read_input_tokens`）。如果重复请求相同前缀时它一直是 0，说明存在上面某种静默失效。这类问题不会报错，只会让账单变高，所以要在测试中加一条断言：第二次相同请求的缓存读取必须大于 0。

#### 阶段项目

完成 V3 聊天后端：

```text
POST /conversations
POST /conversations/{id}/messages
GET  /conversations/{id}/messages
POST /chat/stream
```

本阶段结束时，用已有的前端能力为这个后端接上一个可用的聊天界面并部署上线。目的不是学习前端，而是尽早得到一个"打开链接就能用"的完整产品形态，后续所有阶段都在这个可演示的基础上继续迭代。

#### 阶段验收

- 能保存并恢复多轮对话
- 能实现稳定的流式返回
- API 密钥不会写入代码或提交到版本库
- 能处理模型超时、限流和返回格式异常
- 能记录每次调用的模型、耗时和 Token 使用量
- 能确认 Prompt 缓存确实命中，并能定位一次缓存失效的原因
- 能追溯一次回答使用的 Prompt 版本和模型参数
- 能在客户端中断流式请求后正确释放资源
- 测试不依赖每次调用真实模型

### 阶段 5：Agent 核心与评测

> **AI 强度：写松审严** —— 工具函数可以让 AI 写，但 Agent 循环第一版、评测用例和确认边界必须自己完成。做一次三版对照实验：两个 agent 各写一版循环，自己写一版，逐条比较差异。详见第十一节。

#### 核心判断

固定的“搜索文档 → 调用模型 → 返回答案”是工作流。只有当模型可以在运行时决定是否调用工具、调用哪个工具、是否继续以及何时停止时，才进入 Agent 的核心部分。

#### 学习内容

- Function Calling / Tool Calling
- 工具定义与参数 Schema
- 工具注册和执行
- 工具级权限和动态启用条件
- 把模型输出和工具返回都视为不可信输入
- 最小 Agent 循环
- 最大循环次数
- 单步和整次运行超时
- 工具错误返回
- 幂等性
- Agent 状态、持久化检查点与执行轨迹
- 人工确认与暂停恢复
- 输入、输出和工具 Guardrails
- MCP 的基本结构、传输方式和最小权限原则
- 先手写最小循环，再学习 Agent 框架

#### 最小 Agent 循环

```text
接收用户问题
→ 把可用工具提供给模型
→ 模型决定是否调用工具
→ 后端校验并执行工具
→ 将结果交还模型
→ 模型决定继续调用或生成答案
→ 达到答案、次数或时间边界后停止
```

#### 同步建立评测集

评测集与 Agent 循环同时建立，不推迟到阶段 6。

原因是 Agent 与普通后端的关键差别在于"能跑"和"跑得对"是两件事。没有可重复执行的评测，修改 Prompt、工具描述或模型参数之后无法判断是变好还是变坏，这个阶段会退化成盲目调参。

起步规模不需要大，一个 pytest 文件、10 到 15 个固定用例即可，至少覆盖：

- 是否选择了正确的工具
- 是否提供了正确的工具参数
- 信息不足时是否承认不知道
- 工具执行失败后是否安全停止

模型调用在评测中使用录制好的响应或桩数据，保证可以零成本、可重复地反复执行。完整的评测维度在阶段 6 继续扩展。

#### 阶段验收

- 能不依赖复杂框架实现一个最小工具调用循环
- 能校验工具参数和处理工具执行失败
- 能防止 Agent 无限循环
- 写入、删除和外部操作具有人工确认边界
- 能从数据库还原一次 Agent 运行过程
- Agent 在进程中断后可以从持久化检查点安全恢复
- 能安全接入一个只读 MCP 工具，并说明其权限边界
- 具备一组可重复执行的评测用例，修改 Prompt 或工具定义后能立即回归验证

### 阶段 6：记忆、可观测性、安全与部署

> **AI 强度：全放开** —— Dockerfile、CI 配置、OTel 接入、限流和部署脚本全部让 AI 写；自己负责读懂每一行，并决定观测什么、评测什么。详见第十一节。

#### 记忆与检索

记忆不等于 RAG，也不等于把所有消息存进向量数据库。按照数据性质选择存储方式：

| 数据 | 初始方案 |
|---|---|
| 用户姓名、偏好、当前状态 | PostgreSQL 结构化字段 |
| 最近对话 | 会话消息与上下文窗口 |
| 学习任务、错题、进度 | PostgreSQL 关系表 |
| 历史笔记 | 全文搜索和条件查询 |
| 语义相近的笔记片段 | 评测证明有必要后使用 pgvector |
| PDF、图片等原始文件 | 文件系统或对象存储 |

长期记忆需要支持来源、创建时间、更新时间、失效、修改和删除，不能只支持不断添加。

#### 最小 RAG 实现

RAG（检索增强生成）指的是：先从自己的资料里检索出相关片段，把它拼进 Prompt 再交给模型回答，从而让模型基于你的笔记而不是它的训练记忆作答。

前面的表格已经说明，本项目的大部分检索需求用 PostgreSQL 的条件查询和全文搜索就能满足，语义检索要等评测证明必要之后再上。这个判断不变。但仍然需要动手做一个最小版本，原因有两个：一是不实现一遍就无法判断它到底解决了什么问题，二是这是招聘中出现频率最高的名词之一，需要能讲清楚。

最小实现只包含五步：

```text
笔记切分成片段
→ 调用 Embedding 接口把片段转成向量
→ 存入 pgvector
→ 查询时把问题也转成向量，取最相近的若干片段
→ 拼进 Prompt，并在回答中标注来源
```

真正要理解的是它的失败模式，而不是这五步代码：

- 切分粒度不当：切太碎丢失上下文，切太大引入噪声
- 检索到的片段语义相近但实际无关，反而误导模型
- 检索不到时模型不承认不知道，转而编造
- 无法解释某个答案究竟来自哪一段笔记
- Embedding 模型更换后，旧向量全部需要重新生成

**验收要求**：能用阶段 5 的评测集对比"加 RAG"和"只用全文搜索"两组结果，并说明在本项目里为什么选择了其中一种。能说清"为什么先不用"，比会写这五步更有价值。

#### 评测扩展

在阶段 5 已建立的评测集基础上继续扩展，最终至少覆盖：

- 是否选择了正确工具
- 是否提供了正确工具参数
- 是否错误调用了危险工具
- 是否正确使用笔记来源
- 信息不足时是否会承认不知道
- 多轮对话是否保持上下文
- 失败后是否能够安全停止
- 延迟和 Token 成本是否可接受

生产中的失败案例应补充到回归测试集中。

#### 可观测性

使用统一标识把一次请求的完整链路串起来：

```text
Request ID
→ User / Conversation ID
→ Agent Run ID
→ Model Call
→ Tool Call
→ Database Query
```

至少建立四类观测能力：

- Logs：结构化日志和错误上下文
- Metrics：请求量、错误率、延迟、Token 和费用
- Traces：API、模型、工具和数据库的调用链路
- Evals：Agent 行为质量和回归结果

学习 OpenTelemetry 的基本概念，先实现关键链路，再根据部署平台选择日志、指标和追踪后端。

#### 安全与生产工程

- 用户认证与权限控制
- API 限流
- Prompt Injection 基础防护
- 工具输入、输出和副作用的权限控制
- 敏感信息与日志脱敏
- 数据访问范围控制
- CORS 配置
- 密钥和环境配置管理
- 数据保留、导出和删除边界
- FastAPI `BackgroundTasks` 与外部任务队列的选择边界
- Redis 或消息队列只在出现重任务、跨进程任务或可靠重试需求后引入
- 健康检查
- Docker
- CI：格式检查、静态检查、测试和迁移检查
- CD：构建、部署、健康验证和回滚
- 基础并发与负载测试
- 数据库备份
- 依赖更新与漏洞检查

#### 阶段验收

- 新用户可以注册、登录并访问自己的会话
- 不同用户的数据不会互相泄露
- 能通过 Request ID 和 Agent Run ID 追踪一次完整请求
- 能查看请求量、错误率、延迟、Token 和费用指标
- 固定评测集可以重复执行
- CI 会自动执行格式检查、测试和迁移检查
- 项目可以通过 Docker 在新环境启动
- 能完成部署、健康验证和一次回滚演练
- 能根据日志、指标和 Trace 排查一次真实错误
- 能说明用户数据如何导出、删除和按期限清理

## 七、推荐技术栈

技术栈随学习阶段逐步引入，不需要第一天全部安装。

```text
Python
uv
Ruff
FastAPI
Pydantic
Pydantic Settings
SQLAlchemy 2.x
Alembic
SQLite
PostgreSQL
pytest
HTTPX
大模型官方 Python SDK
Docker
OpenTelemetry（生产阶段）
pgvector（按需）
Redis（按需）
MCP（Agent 工具互操作阶段）
```

Agent 框架不在最初的必选项中。手写并理解最小 Agent 循环后，再根据项目复杂度评估 OpenAI Agents SDK、LangGraph 或其他框架。

## 八、每周学习节奏

每周 8～10 小时可以这样分配：

| 活动 | 比例 | 目的 |
|---|---:|---|
| 阅读和理解 | 30% | 建立基本概念 |
| 亲手编码 | 50% | 把知识放进项目 |
| 调试、测试、复盘 | 20% | 形成独立解决问题的能力 |

每个知识点遵循同一个循环：

```text
理解概念（可以问 AI）
→ 自己写最小示例          ← 阶段 1～3 不借助 AI
→ 让 AI 审阅并挑毛病
→ 放进主项目
→ 主动测试错误情况
→ 编写测试
→ 用自己的话记录原因
→ 把记录讲给 AI 听，让它指出理解错误的地方
```

最后一步价值最高。能不能讲清楚比能不能写出来更接近"掌握"的定义，而这恰好是 AI 最容易验证的事情。各阶段的 AI 使用强度见第十一节。

每周至少保留下面四项记录：

- 本周完成了什么可运行功能
- 遇到了什么错误
- 错误的真正原因是什么
- 下周准备验证什么

## 九、第一周执行计划

第 1～2 天完成阶段 0，第 3 天起进入阶段 1 的 Python 语言学习。终端、路径、Git 和包管理概念已经具备，阶段 0 只需要确认 Python 生态中的对应工具，不重新建立概念。

### 第 1 天：Python 项目与 uv

- 确认 Python 版本与安装位置，理解全局解释器与虚拟环境的关系
- 安装 `uv`，创建项目和虚拟环境
- 对照 `package.json` 理解 `pyproject.toml`
- 对照 lock 文件理解 `uv.lock` 与可重复安装
- 区分项目依赖和开发依赖
- 认识 `main.py`、`tests/` 等最小项目结构
- 添加并运行一个第三方依赖

成果：能用 `uv` 创建项目、安装依赖并运行代码，并能说明当前代码运行在哪个解释器上。

### 第 2 天：工具链、安全与阶段 0 验收

- 添加 Ruff，运行格式化和基础检查，对照 Prettier 与 ESLint 的分工
- 创建 `.gitignore`，确认 `.env`、密钥和虚拟环境不会被提交
- 初始化 Git 并完成第一次提交
- 主动制造语法错误、名称错误和一个运行时错误，从堆栈中定位错误类型、文件和行号
- 从空目录重新完成一次项目创建与运行

成果：完成阶段 0 验收，得到一个可以安全提交、能够重复安装依赖的最小项目。

### 第 3 天：基本类型与容器

- 字符串、整数、浮点数、布尔值和 `None`
- 列表、元组、字典、集合，以及与 JavaScript 数组和对象的差异
- 缩进语法、真值判断和基本运算
- 使用变量和字典保存一条学习任务

成果：打印一条结构清楚的学习任务。

### 第 4 天：条件、循环与输入

- `if/elif/else`、`for` 和 `while`
- 列表推导式
- 使用 `input()` 接收用户输入并转换类型
- 对无效输入给出提示

成果：根据用户输入创建或拒绝一条学习任务，并能列出多条任务。

### 第 5 天：函数与类型标注

- 函数、参数、默认值、返回值和作用域
- 位置参数与关键字参数
- 可变默认参数陷阱
- 为函数添加类型标注，并运行一次类型检查

成果：把前几天的代码重构成若干职责明确的函数。

### 第 6 天：模块、异常与文件

- 模块、包和导入，把程序拆分成多个文件
- `try/except` 与常见内置异常
- 使用 `pathlib` 读写文件
- 使用 `json` 保存和读取任务列表

成果：任务数据可以保存到 JSON 文件，并在下次运行时恢复。

### 第 7 天：测试与第一周复盘

- 编写第一个 pytest 测试，覆盖正常、异常和边界情况
- 不看原代码重新实现本周的核心函数
- 记录本周最难理解的三个问题
- 运行 Ruff，并提交第一周版本

成果：一个可保存数据、带基本测试的命令行任务程序雏形，为第二周继续完善 V0 做好准备。

## 十、暂时不优先学习的内容

在主项目出现真实需要之前，暂时不把时间投入到：

- 算法竞赛和大量刷题
- 数据分析、爬虫和桌面 GUI
- Python 高级元编程
- 微服务和 Kubernetes
- 多 Agent 协作
- 复杂 LangGraph 状态图
- 一开始就安装专用向量数据库
- 同时接入多个 Agent 框架
- 没有评测依据的复杂 RAG

这些内容并非不重要，而是需要在合适的阶段学习。

## 十一、AI 协作方式

本节说明在什么时候使用 AI、使用它做什么、以及每个阶段的强度差异。

日常使用 Claude Code 和 Codex，两者都是能读取仓库的编码 agent，能力大量重叠，互为替代而非互补。不必人为划分职责，按顺手选即可，但有一条硬要求：**两边的约定文件必须是同一份**，否则同一个问题在两个工具里会得到深度不同的回答，反而制造困惑。约定文件的组织方式见本节末尾。

同时使用两个工具的额外收益体现在阶段 5 的三版对照实验上，见下方强度表。

### 一条红线

> 能运行但不能解释，就不算掌握。

这是唯一的判定标准。不规定哪些代码必须手写，只规定结果：无论代码由谁写出，都必须能够逐行说明为什么这样写、换一种写法会有什么后果。做不到就回退重学，而不是继续往前推进。

### 两类工作

计划中的内容分成两类，归属规则不同。

| 类型 | 例子 | 归属 |
|---|---|---|
| **决策** | 数据模型设计、错误边界划分、工具接口形态、哪些操作需要人工确认、评测覆盖什么、某条查询为什么慢 | 永远由自己完成。AI 只能提供对照方案，不能替代选择 |
| **实现** | CRUD 骨架、Pydantic 模型、迁移脚手架、Dockerfile、CI 配置、类型标注补全 | 按阶段决定由谁完成，见下方强度表 |

区分的依据是：手写实现代码本身教不了什么，做出并解释一个设计决策才是学习发生的地方。

### AI 的五个角色

按介入时机划分，不同时间点应该向 AI 要不同的东西。

| 时机 | 角色 | 要什么 | 不要什么 |
|---|---|---|---|
| 学习新概念时 | 讲解者 | 解释和前端类比 | 不要示例代码 |
| 动手之前 | 陪练 | 出题、故意埋 bug 让自己找 | 不要标准答案 |
| 卡住 15 分钟后 | 诊断者 | 讲清错误的真正原因 | 不要直接给修复 |
| 写完之后 | 审阅者 | 挑毛病、指出不地道的写法 | 不要让它动手改 |
| 面临选择时 | 方案顾问 | A、B 方案各自的取舍 | 不要它替自己选 |
| 确认无学习价值时 | 代工 | 直接写完 | —— |

"卡住 15 分钟"是一个下限，不是上限。困惑本身是学习的必要条件，提前消除困惑等于跳过学习。

### 按阶段的强度

| 阶段 | 强度 | AI 可以做 | 必须自己做 | 本阶段专属动作 |
|---|---|---|---|---|
| **0** 环境（2 天） | 放开 | 用前端类比讲解工具链、排查安装问题 | 亲手跑一遍每个命令 | 要求它对照 npm 讲 uv、对照 ESLint 讲 Ruff，不要从零讲起 |
| **1** Python（2.5 周） | **最严** | 解释错误、出题、埋 bug、审阅 | **全部代码** | 每天结束把当天代码贴给它，只问一个问题："哪里不像 Python" |
| **2** FastAPI（2 周） | 严 | 配置与测试脚手架、讲解 FastAPI 内部机制 | 路由设计、请求响应模型、错误结构 | 先自己设计完整 API，再让它以挑剔的后端评审身份提意见 |
| **3** 数据库（4 周） | 中 | 写查询、生成迁移脚本、构造测试数据 | 表结构、关系、索引决策 | 先自己画 Schema 再让它找问题；让它造一个 N+1 查询，自己去发现。迁移脚本必须逐行读懂后才执行 |
| **4** 大模型（3 周） | 中高 | SDK 调用、流式处理骨架、**前端界面完全交给它** | 模型访问层的边界、超时与重试策略、缓存前缀如何排列 | 缓存命中必须自己验证——这是全计划唯一不会报错的失败 |
| **5** Agent（6 周） | **写松审严** | 工具函数实现、参数 Schema 定义 | Agent 循环的第一版、评测用例、人工确认边界 | 三版对照实验：两个 agent 各写一版循环，自己写一版，逐条比较差异 |
| **6** 生产工程（6 周） | 全放开 | Dockerfile、CI 配置、OTel 接入、限流、部署脚本，全部 | 读懂每一行；决定观测什么、评测什么 | 让它写完后主动去破坏它——破坏不掉说明还没真正理解 |

三个阶段需要额外说明。

**阶段 1 为什么最严。** 这是全计划唯一以"建立语言手感和调试直觉"为目标的阶段。让 AI 代写等于没有学。已有的前端经验会让 Python 代码带上明显的 JavaScript 口音——用索引循环而不是 `enumerate`、该用 `dataclass` 的地方堆字典、习惯性地写 `for...in` 遍历下标。这类问题自己看不出来，但 AI 一眼就能指出，所以"哪里不像 Python"是这个阶段性价比最高的一句提问。

**阶段 5 为什么是"写松审严"。** 这是唯一强度不随阶段线性放松的地方。工具函数让 AI 写没有问题，但 AI 生成的 Agent 循环第一版经常是似是而非的：能跑通正常路径，却漏掉最大循环次数、单步超时、工具执行失败的返回处理。这些缺陷在演示时不会出现，在生产里必然出现。

对照实验的价值就在这里。让两个 agent 各写一版循环，自己再写一版，然后逐条比较三份代码的差异。两份对照能暴露"它考虑到而自己没想到的"和"它漏掉而自己必须补上的"；三份对照多暴露一类信息——**两个模型共同漏掉的东西**。那通常不是某一个模型的疏忽，而是这类任务真正的坑，也是最值得写进评测集的用例。

**阶段 6 为什么全放开。** 手写 YAML 和 Dockerfile 没有任何学习价值，这个阶段的能力体现在"知道要观测什么"，而不是"能背出配置语法"。

### AI 在每周循环中的位置

第八节的知识点循环中有两步与 AI 相关：写完最小示例后让它审阅挑毛病，以及复盘时把自己的记录讲给它听、让它指出理解错误的地方。这两步在阶段 1～3 尤其重要，因为那时代码全部由自己编写，AI 是唯一的即时反馈来源。

### AI 与学习如何相辅相成

AI 不是学习的替代品，是**反馈回路的加速器**。

自学最大的瓶颈从来不是资料不够，而是没有人指出哪里想错了。这个反馈过去依赖代码评审、同事或线上事故，延迟以月计；AI 把它压缩到秒级。具体通过三个机制起作用：

- **缩短反馈延迟**：写完立刻知道哪里不地道，而不是三个月后才发现
- **提供对照组**：自己的方案与它的方案之间的差异，就是当前的知识缺口
- **强制表达**：讲给它听并被挑错，是检验理解最快的方式

而 AI 伤害学习只有一种情况：**它替你完成了本该由你产生困惑的那一步。** 上面所有规则都是这一条的推论。

### 三条反模式

- 卡住不到 15 分钟就提问——跳过了困惑
- 让它一次生成整个阶段项目——跳过了全部决策
- 接受了能跑的代码却没让它解释——违反红线

### 约定文件

两个工具都会自动读取项目根目录下的约定文件：Codex 读 `AGENTS.md`，Claude Code 读 `CLAUDE.md`。把学习模式写进去，约束就默认生效，不依赖每次记得克制自己。

**保持单一来源，不要复制两份。** 内容写进 `AGENTS.md`，`CLAUDE.md` 只写一行引用：

```markdown
@AGENTS.md
```

Claude Code 支持用 `@路径` 导入其他文件。这样换阶段时只改一处。也可以用软链接（`ln -s AGENTS.md CLAUDE.md`）达到同样效果，但指针文件在 GitHub 上打开时更能看出意图。

复制两份的后果是必然失同步：下面模板里"当前阶段"那一行每个阶段都要改，两份文件迟早会不一致，然后同一个问题在两个工具里得到深度不同的回答。

**配置完成后验证一次**：分别用两个工具开一次会话，问"你读到了什么协作约定"。两边都能复述出规则才算配置成功。这一步一分钟，但配错了不会有任何报错提示，只会让约束静默失效。

第一周建好 Python 项目后，将下面内容放进 `AGENTS.md`：

```markdown
# 协作约定

我正在按 24 周计划学习 Python 与 AI Agent 后端开发。
我有完整的前端开发经验，Python 零基础。

## 当前阶段

阶段 1（Python 核心基础），AI 强度：最严。

## 规则

- 不要直接给出完整实现。先反问我打算怎么做，等我说完再评价。
- 解释错误时讲清楚原因，不要直接给修复代码。
- 需要写代码时，只给最小示例，不要给可以直接粘进项目的完整版本。
- 审阅我的代码时重点指出「不像 Python 的写法」，我会带 JavaScript 口音。
- 涉及设计决策时给出 A、B 方案的取舍，不要替我选。
- 我说「我懂了」之后，让我复述一遍，指出我讲错的地方。

## 讲解方式

用前端概念类比：npm 对应 uv，zod 对应 Pydantic，
ESLint 加 Prettier 对应 Ruff，Express 路由对应 FastAPI 路由。
已经掌握的内容不要从零讲起：HTTP、REST、JSON、状态码、Git、包管理。
```

随阶段推进直接修改 `AGENTS.md` 来放松约束——把"当前阶段"一行改掉，并按上方强度表调整规则。进入阶段 6 时，这些规则基本可以全部删除。

## 十二、参考资料

- [Python 官方教程](https://docs.python.org/3/tutorial/)
- [FastAPI 官方教程](https://fastapi.tiangolo.com/tutorial/)
- [Pydantic 官方文档](https://docs.pydantic.dev/latest/)
- [SQLAlchemy 2.0 官方教程](https://docs.sqlalchemy.org/en/20/tutorial/)
- [PostgreSQL 官方文档](https://www.postgresql.org/docs/current/)
- [pytest 官方文档](https://docs.pytest.org/)
- [OpenTelemetry Python 官方文档](https://opentelemetry.io/docs/languages/python/)
- [Model Context Protocol 官方文档](https://modelcontextprotocol.io/docs/)
- [OpenAI Agents SDK 官方文档](https://openai.github.io/openai-agents-python/)
- [LangGraph 官方文档](https://docs.langchain.com/oss/python/langgraph/)

参考资料用于解决当前问题，不以从头到尾读完文档为学习目标。

## 十三、总进度清单

- [ ] 能独立运行和调试 Python 程序
- [ ] 完成命令行学习记录程序
- [ ] 完成 FastAPI 学习任务 API
- [ ] 能正确管理 FastAPI 生命周期、配置和异步资源
- [ ] 能演示并修复一次异步路由中的阻塞问题
- [ ] 掌握 SQLite 和基本 SQL
- [ ] 将项目切换到 PostgreSQL
- [ ] 完成数据库迁移、连接池管理和接口测试
- [ ] 接入大模型并实现流式聊天
- [ ] 为聊天后端接入前端界面并部署上线
- [ ] 能追溯 Prompt、模型参数、Token、延迟和费用
- [ ] 让 Prompt 缓存稳定命中，并能定位一次缓存失效
- [ ] 独立实现最小 Agent 工具调用循环
- [ ] 建立 Agent 固定评测集，并可重复执行
- [ ] 实现 Agent 持久化检查点、暂停与恢复
- [ ] 安全接入一个只读 MCP 工具
- [ ] 完成知识库搜索工具
- [ ] 建立会话和长期记忆边界
- [ ] 完成最小 RAG 实现，并用评测说明是否值得保留
- [ ] 完成鉴权、限流和安全控制
- [ ] 建立日志、指标和 Trace
- [ ] 建立基础 CI/CD 和回滚流程
- [ ] 使用 Docker 部署完整后端
- [ ] 独立完成一次线上问题排查
